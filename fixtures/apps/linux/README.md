# Linux fixture

Run on the declared Ubuntu 24.04 GNOME Xorg runner with `python3 crosshands_fixture.py --oracle <private-absolute-path> --instance a`. Tk uses native AT-SPI exposure through the desktop bridge. `python3 -m py_compile crosshands_fixture.py` is the source/build smoke; interactive behavior is evidence only when driven through final installed CrossHands packages.
